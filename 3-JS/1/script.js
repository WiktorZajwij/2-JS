/*
Завдання 1 — Створіть програму, яка обчислює факторіал натурального числа n, яке користувач введе з клавіатури.
*/

let n = +prompt('Введить число');
let res = 1;

for (let i = 1; i <= n; i++) {
  res = res * i;
  console.log(res);
  document.write(`${res}` + ' ');
}
